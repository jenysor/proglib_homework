# ДЗ №2

Реализация интерфейсов: [interfaces](interfaces/include/interfaces.h).

Реализация команд MoveCommand, StartMoveCommand, EndMoveCommand: [movable](movable/).

Реализация адаптеров: [adapters](adapters/).

Запуск тестов настроен через через github-actions (этап Run tests with covarage).

Ссылка на workflow: [Run tests](https://github.com/jenysor/proglib_homework/actions/workflows/run_tests.yml).